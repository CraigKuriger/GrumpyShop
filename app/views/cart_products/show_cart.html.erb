<!DOCTYPE html>
<html lang="en">

<head>
<title>Qwertyshop</title>

</head>

<body>
<div class="container">

        <div class="row">
            <div class="col-lg-12">
                <h3>Hello <%=current_user.username%>, you have <%=@product.length%> items in your cart</h3><br>
                <% if @total == 0 %>
                <h3>Your shopping cart is empty</h3>
                <% else %>
                <h3>Total: $<%=@total%></h3><br>
                <% end %>
            </div>
        </div>
        <div class="row text-center">

            <%@product.each do |product|%>
            <div class="col-md-3 col-sm-6 hero-feature">
                <div class="thumbnail">
                    <img src="<%=product.image_url%>" alt="">
                    <div class="caption">
                        <h3><%=product.name%></h3>
                        <h3>$ <%=product.price%></h3>
                        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit.</p>
                        <p>
                            <%=link_to "Remove", remove_product_path(product.id), method:"post", class:"btn btn-primary"%>

                        </p>
                    </div>
                </div>
            </div>
            <%end%>
        </div>
        <hr>
</body>
</html>

